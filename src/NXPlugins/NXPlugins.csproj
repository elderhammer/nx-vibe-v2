<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="15.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props" Condition="Exists('$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props')" />
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Debug</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <ProjectGuid>{6C2D1A34-9B4E-4F7A-9E1B-3A8F4C2D5E61}</ProjectGuid>
    <OutputType>Library</OutputType>
    <RootNamespace>NXPlugins</RootNamespace>
    <AssemblyName>NXPlugins</AssemblyName>
    <TargetFrameworkVersion>v4.8.1</TargetFrameworkVersion>
    <FileAlignment>512</FileAlignment>
    <AutoGenerateBindingRedirects>true</AutoGenerateBindingRedirects>
    <!-- NX 安装根：可用环境变量 NX_DIR 覆盖；程序集引用为私有引用（copy local=false），不随工程分发 -->
    <NX_DIR Condition=" '$(NX_DIR)' == '' ">C:\Program Files\Siemens\NX2406</NX_DIR>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
    <ErrorReport>prompt</ErrorReport>
    <WarningLevel>4</WarningLevel>
  </PropertyGroup>
  <ItemGroup>
    <Reference Include="System" />
    <Reference Include="System.Core" />
    <Reference Include="System.Runtime.Serialization" />
    <Reference Include="System.Xml" />
  </ItemGroup>
  <ItemGroup>
    <!-- NXOpen .NET 引用（NX 2406，仅支持 2406——决策②）。hintpath 指向本机安装，勿拷入仓库 -->
    <Reference Include="NXOpen">
      <HintPath>$(NX_DIR)\NXBIN\managed\NXOpen.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <Reference Include="NXOpen.UF">
      <HintPath>$(NX_DIR)\NXBIN\managed\NXOpen.UF.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <Reference Include="NXOpen.Utilities">
      <HintPath>$(NX_DIR)\NXBIN\managed\NXOpen.Utilities.dll</HintPath>
      <Private>False</Private>
    </Reference>
  </ItemGroup>
  <ItemGroup>
    <Compile Include="Properties\AssemblyInfo.cs" />
    <!-- 生产代码：探针/适配器 Journal（NX 会话执行入口，含 Main，类库编译无碍）、
         PlanExporter 与 PlanExecutor 纯逻辑核心（零 NX 依赖仅对 csc 合并场景成立；
         本工程引用 NXOpen 全家，入库编译无碍）。测试目录 PlanExporterTests/PlanExecutorTests
         （自定义 Runner + Assert）不入产品库，走 scripts/run-unittests.ps1 独立红线回归。 -->
    <Compile Include="Journal\*.cs" />
    <Compile Include="PlanExporter\*.cs" />
    <Compile Include="PlanExecutor\*.cs" />
    <Compile Include="PlanComparer\*.cs" />
  </ItemGroup>
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
</Project>
